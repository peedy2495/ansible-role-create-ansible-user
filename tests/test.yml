---
- name: Test the create_ansible_user role
  hosts: targets
  gather_facts: true
  become: true
  roles:
    - role: "{{ playbook_dir }}/.."
      create_ansible_user_ssh_public_keys:
        - "ssh-ed25519 AAAA_REPLACE_WITH_A_REAL_TEST_KEY galaxy-role-test"
