---
galaxy_info:
  namespace: ansible_common
  role_name: create_ansible_user
  author: ansible-common contributors
  description: Create a key-only Ansible automation user with full sudo access.
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions:
        - all
    - name: EL
      versions:
        - "8"
        - "9"

dependencies: []
